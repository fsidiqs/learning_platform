import Button from '@material-ui/core/Button';

const WatchCourseButton = (props) => {
   const AddToCartButton = <Button>Add To Cart</Button>;

   return WatchCourseButton;
};

export default AddToCartButton;
